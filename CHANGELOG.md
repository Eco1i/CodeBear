# 更新记录

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 1.5.16 - 2026-08-29

### 新功能

- 表详情支持同时打开多个数据表标签，点击表关系或搜索结果时可直接切换或新增标签。
- 切换标签时保留各表的编辑草稿，并显示未保存修改标记。

### 修复与体验改进

- 搜索结果不再自动打开第一行，只有用户主动点击后才会记录“最近打开”。
- 已打开的标签页和当前激活标签会在刷新页面后恢复。
- 搜索记忆每个搜索上下文最多提升 10 张最近打开的数据表。
- 支持关闭当前、关闭其他和关闭右侧标签。
- 关闭有未保存修改的标签时，可选择保存并关闭、放弃修改或继续编辑。
- 标签页支持键盘切换和关闭，窄窗口下保持横向滚动。

## 1.5.15 - 2026-08-29

### 新功能

- 新增搜索记忆：会根据当前项目和搜索模式记住最近打开的数据表，在后续搜索中优先展示相关结果。
- 搜索记忆默认开启，可在搜索偏好中关闭或清除；记忆最多保留 100 条，保存 90 天，并最多提升 3 张相关数据表。

### 修复与体验改进

- 优化搜索结果排序，精确匹配、最近打开且匹配、前缀匹配和描述/注释匹配的优先级更加清晰。
- 修复 Windows 绿色版退出后后台服务进程、端口或单实例状态未及时释放的问题。
- Windows 版在关闭请求较慢时会在有限等待后完成退出，避免程序长期残留。

## 1.5.5 - 2026-08-29

### 修复

- 修复浮动模式关闭 AI 助手后，悬浮入口短暂被遮挡或显示异常的问题。
- 关闭助手后，悬浮入口会在面板完全退出后正常显示；助手打开时不会遮挡对话内容。

## 1.5.4 - 2026-08-26

### 修复

- 修复关闭 AI 助手后悬浮入口仍从半透明状态二次淡入、在白色工作区上看起来像被矩形内容层遮住的问题；入口现在等待助手退出后直接以完整状态恢复。
- 关闭助手后，悬浮入口会以完整状态稳定显示，不再出现短暂遮挡感。

## 1.5.3 - 2026-08-26

### 修复

- 重做数据表删除确认窗口，使用与当前工作台一致的紧凑标题栏、目标表摘要、影响统计和自动备份提示，删除范围更清楚。
- 修复关闭 AI 助手时入口与退出面板仍会出现短暂叠盖的问题；入口改为等待面板真实完成可见性切换后再恢复，并保留异常兜底。
- 移除操作列额外的深色边界与阴影，分隔线统一为普通列使用的 1px 浅色线，操作列不再显得突兀。
- 补充删除窗口的无障碍命名，图标不再混入对话框标题朗读。

## 1.5.2 - 2026-08-26

### 修复

- 修复窄工作区内数据表“操作”列被固定最小表宽推到可视区外、导致单表删除入口完全不可见的问题；选择列和操作列固定占位，中间字段按可用宽度压缩并保留省略展示。
- 加强操作列的稳定分隔和删除按钮可见性，窗口宽度变化时不再依赖横向滚动寻找行操作。
- 修复关闭 AI 助手时悬浮入口过早出现、被退出中的助手面板短暂遮挡或裁切的问题；入口会在 220ms 退出动画结束后恢复，减少动态效果模式下立即恢复。

## 1.5.1 - 2026-08-26

### 体验改进

- AI 助手悬浮入口支持鼠标、触控笔和触屏自由拖动，位置会保存在当前浏览器中。
- 拖动位置限制在窗口可视范围内；窗口尺寸变化后会自动校正，避免入口移出屏幕。
- 增加拖动阈值，普通点击仍会打开助手，完成拖动时不会误触发打开。
- 支持使用 `Alt + 方向键` 移动入口，按住 `Shift` 时可加速移动，并补充键盘与屏幕阅读器提示。

## 1.5.0 - 2026-08-26

### 新功能

- “编辑字典”新增字段创建与删除能力；新增字段可直接维护英文名、描述、类型、长度、可空、缺省值和备注，并与原有修改一起写回 PDM。
- 数据表列表新增单表删除和复选批量删除，删除前展示涉及的表、字段、PDM、表关系及字典绑定数量。
- 表删除会同步清理 PDM 原生外键关系和图形符号，支持一次安全处理多个 PDM。

### 安全与体验

- 字段英文名和数据类型增加必填校验，字段英文名按大小写不敏感规则检查重复。
- 主键字段、被表关系引用或绑定业务字典的字段禁止直接删除，并给出解除依赖提示。
- 字段结构变更和数据表删除都会先保留 PDM 原文件备份；写回或索引更新失败时自动回滚。
- 删除请求校验打开时的 PDM 内容哈希，文件已变化时停止操作，避免覆盖外部修改。
- 数据表复选、批量操作条、删除按钮和字段行操作沿用现有紧凑型工作台视觉规范。

## 1.4.0 - 2026-08-21

### 本次更新

- 新增 macOS 桌面版，支持 macOS 13 及以上 Apple Silicon 设备。
- Mac 版从菜单栏运行，可以打开工作台、数据目录或检查更新。
- 更新功能会根据当前系统提供对应的 Windows 或 Mac 安装包。
- Windows 版继续提供绿色 ZIP，原有功能和使用方式不变。

### 使用说明

- Windows 和 macOS 使用两个独立安装包，不能混用。
- Mac 版只支持 M 系列芯片，暂不支持 Intel Mac。
- Windows 数据保存在程序同级的 `data` 目录；macOS 数据保存在 `~/Library/Application Support/CodeBear`。
- 当前 Mac 版未经过 Apple 公证，首次启动时可能需要在 Finder 中右键选择“打开”，或在“系统设置 > 隐私与安全性”中允许打开。

## 1.3.1 - 2026-08-16

### 修复

- 修复导出建表脚本单次最多 5,000 张表的限制：项目包含大量数据表（如交易系统 7,000+ 张）时全选导出会报错，现上限提升至 50,000 张。
- 优化接口参数校验错误的提示：不再笼统显示“请求失败（422）”，改为展示具体校验原因。
- 收紧 .gitignore，排除本地存放业务字典的目录，避免误提交生产数据。

## 1.3.0 - 2026-08-15

### 新功能

- 新增表关系浏览：自动解析 PDM 外键引用，字段面板的表代码以橙色标注并可点击打开关系抽屉；支持手工新增、编辑、删除表关系，与自动解析的关系并列展示。
- 关系抽屉采用一行式极简列表（方向箭头 + 相关表 + 字段映射），按入向/出向分组并支持折叠，提供页签与搜索过滤；详情收进悬停浮层。
- 关系图采用气泡圆 + 连线布局：表代码写在圆内，入向在左、出向在右，关系多时自动退化为圆点星图；点击圆或连线聚焦高亮、其余变暗淡，双击相关表切换中心，悬停连线查看外键字段；支持缩放、平移与拖拽调整，带入场动画。
- 手工维护的表关系随 `.cbbak` 备份迁移，导出时可勾选是否包含（自动解析关系导入后由 PDM 重新生成，不占体积）。
- 强制刷新显示实时进度：进度条展示当前解析的 PDM 与完成数；按内容哈希跳过未变化的 PDM，重复刷新约 2 秒完成；待解析 PDM 并行解析。
- 关系抽屉与关系图的搜索框统一为回车或点击放大镜触发，与全局搜索一致。

### 修复

- 修复部分 PDM 同一对表字段在多个外键引用中重复出现、导致强制刷新报唯一约束冲突的问题，自动去重。
- 修复强制刷新缓慢：将逐行外键级联删除改为显式批量删除（A-交易系统 23 个 PDM 实测约 93 秒降至 15 秒），并为关系表补充单列索引。
- 修复重建 PDM 索引时字典字段绑定被连带删除的问题：重建后按字段原标识恢复绑定，强制刷新不再丢失字典绑定数据。
- 修复关系图双击无法切换中心表的问题（指针事件吞掉原生双击，改为按点击间隔识别）。
- 修复同一对表存在多条关系时连线完全重叠的问题，改为错开弧线。
- 修正新增/编辑关系窗口按钮贴底、字典维护窗口分页挤压分隔线等间距问题。

## 1.2.1 - 2026-08-15

- 顶栏版本徽标高度与相邻按钮对齐（34px），统一圆角与字号，消除视觉不协调。

## 1.2.0 - 2026-08-15

- 新增应用内更新检查：启动时、每 6 小时以及手动三种方式查询 GitHub Releases 的最新稳定版，发现新版本在顶栏以徽标提示。
- 更新面板展示发布说明、官方 SHA-256 与复制按钮，并提供三步升级引导（校验安装包 → 退出旧版并解压新目录 → 备份迁移旧数据）；支持「忽略此版本」。
- 托盘菜单新增「检查更新」入口，点击直接打开更新面板。
- 更新检查只向 GitHub 公共 API 发送匿名 GET 请求，不携带任何本地数据；下载由浏览器直链完成。

## 1.1.2 - 2026-08-15

- 修复 CodeQL 报告的异常信息泄露（`py/stack-trace-exposure`）：备份导入、PDM 导入、刷新索引与 Excel 读取等接口不再把原始异常文本返回给客户端，统一返回通用提示，异常详情写入 `data/logs/server.log` 供排查。
- 修复 README 中残留的旧版本号与旧发行包文件名。

## 1.1.1 - 2026-08-15

- 修复导入 `.cbbak` 备份时，大小写不同的字典值（如 `B|1` 与 `b|1`）被误判为重复、报“字典备份包含空值或重复值”的问题；备份导入与 Excel 导入保持一致的大小写敏感去重。

## 1.1.0 - 2026-08-14

- 使用 SQLite FTS5 trigram 索引加速表、字段和备注搜索，中文短词自动回退原有 LIKE 搜索。
- 优化字段全文搜索查询计划，FTS 命中只执行一次，避免大型数据字典按表重复扫描索引。
- PDM 导入、编辑、删除和恢复时增量同步全文索引，并在索引版本变化或一致性异常时自动重建。
- 备份导入采用单事务批量写入、按 PDM 合并字段入库和受影响范围的 FTS 批量同步，大型备份导入更快且保持失败回滚。
- AI 助手、备份迁移和 DDL 导出改为按需加载，启动阶段不再读取 AI 设置与会话。
- 修复刷新后 AI 助手启动器未恢复个性化名称和配饰的问题，同时保留 AI 主功能懒加载。
- “编辑字典”支持同时修改表名称、表代码和表描述，并与字段内容一起安全写回 PDM。
- 优化数据字典保存，仅同步当前表和实际变化字段的数据库及 FTS 索引，避免整份 PDM 全量重建。
- 修复定位 PDM 后旧定位请求在切换目录或窗口重新聚焦时被重复执行、导致已收起目录再次展开的问题。
- 前端按工作区、数据表、AI、备份和 DDL 拆分 API、类型、纯业务逻辑与 CSS，应用壳仅负责跨域组合。
- 备份导入增加平滑变化的百分比进度条，并在导入期间拦截误刷新或关闭页面。
- 新增字典中心：手工维护数据字典，支持 Excel 批量导入、字段绑定查询，并随 `.cbbak` 备份迁移。
- Excel 导入支持多选字典值列组成唯一组合键（如“方向|市场”），自动跳过完全相同的重复行，同码不同名冲突保留首次出现并计数提示，字典值区分大小写。
- 增加 Vitest、React Testing Library 和 Playwright 回归测试，并接入持续集成。
- 发布流程增加统一版本号校验，确保本地程序、后端与前端均为 1.1.0。

## 1.0.0 - 2026-08-13

首个公开开源基线版本。

- 提供 PDM 项目管理、解析、搜索和安全字段修订。
- 提供回收站、选择性备份、恢复及旧版数据迁移。
- 支持 MySQL、Oracle、达梦、TDSQL MySQL 版和 Apache Ignite DDL 生成。
- 提供可选的 DeepSeek 数据字典问答助手。
- 提供 Windows x64 绿色版、自动构建与解压启动验收。
- 清除私有开发历史、真实业务样本、内部截图和原型稿。
